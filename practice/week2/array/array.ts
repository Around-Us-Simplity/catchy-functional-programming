/**
 * 카배열의 push, pop, shift, unshift, splice 메서드를 카피-온-라이트로 구현하기
 * @param array
 * @returns
 */

export const push = (array: any[]) => {
  return array;
};

export const pop = (array: any[]) => {
  return array;
};

export const shift = (array: any[]) => {
  return array;
};

export const unshift = (array: any[]) => {
  return array;
};

export const splice = (array: any[]) => {
  return array;
};
